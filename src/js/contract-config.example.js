// 前端合约配置模板。
// 部署后由 scripts/deploy.js 自动覆写 src/js/contract-config.js（已被 .gitignore 忽略）。
// 若直接使用本模板，请将地址替换为实际部署地址、ABI 替换为编译产物中的 abi。
export const contractAddress = "";
export const contractAbi = [];
